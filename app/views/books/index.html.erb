<h1>You have <%= @books.count %> books</h1>
<div class="books">
  <%= render 'search_form' %>
  <div>
    <%= render 'books', books: @batch_of_books %>
    <%== pagy_nav(@pagy) %>
    <%= link_to "Add new Book", new_book_path %>
  </div>
</div>
