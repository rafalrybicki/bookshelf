<%= form_with(model: @book) do |f| %>
  <% if @book.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
        <% @book.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :author %>
    <%= f.text_field :author %>
  </div>
  <div>
    <%= f.label :status %>
    <%= f.select :status, Book.statuses.keys %>
  </div>
  <div>
    <%= f.label :categories %>
    <%= f.collection_check_boxes :categories, current_user.categories, :id, :name %>
  </div>
  <div>
    <%= f.label :info %>
    <%= f.text_field :info %>
  </div>
  <br>
  <div>
    <%= f.submit %>
  </div>
<% end %>
